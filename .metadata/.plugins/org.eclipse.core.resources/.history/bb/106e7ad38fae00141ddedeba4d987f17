Titanium.UI.setBackgroundColor('#000');

var mainWindow, titleLabel;

getGeolookup = require('geolookup');
getConditions = require('conditions');

titleLabel = Ti.UI.createTableView({
	top: 15,
	font: roboto,
)};

mainWindow = Titanium.UI.createWindow({
	title: 'Tab 1',
	backgroundColor: '#fff'
});

win1.add(table1);

win1.open();
